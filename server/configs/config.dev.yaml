mysql:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "123456" # 请替换为您的MySQL密码
  dbname: "school_scm"
  config: "charset=utf8mb4&parseTime=True&loc=Local"

server:
  port: 8080

jwt:
  issuer: "gin-vue"
  secret: "a-secure-secret-key-that-is-long-enough" # 强烈建议从环境变量或更安全的地方加载
  expire: 24 # token过期时间，单位：小时
